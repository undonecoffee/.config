
super + d
    dmenu_run
super + u
    bash ~/passwords/bash/screenshot_text-message-ai/init.sh
super + i
    bash ~/passwords/bash/copyText.sh
super + c
    kitty
super + at
    bash $DOTFILES/setTheme.sh black
super + comma
    bash $DOTFILES/setTheme.sh dark

super + alt + q 
    bspc quit
super + alt + x 
    systemctl suspend
super + alt + r
    bspc wm -r
super + z
    bash $DOTFILES/final/bash/func.sh sxhkdrs
super + s
    bspc node -c
super + apostrophe
    pactl set-sink-volume @DEFAULT_SINK@ +5%
super + slash
    pactl set-sink-volume @DEFAULT_SINK@ -5%

super + q
    bspc desktop -f I
super + w
    bspc desktop -f II
super + e
    bspc desktop -f III
super + r
    bspc desktop -f IV
super + t
    bspc desktop -f "t-term"
super + space
    bspc desktop -f "space-code"
super + p
    bspc desktop -f "p-btop"
super + y
    bspc desktop -f "y-testing"
super + o
    bspc desktop -f "o-obsidian"
super + b
    bspc desktop -f "b-firefox"
super + a
    bspc desktop -f "a-chatgpt"
super + g
    bspc desktop -f "g-github"
super + v
    bspc desktop -f "v-monkeytype"
super + m
    bspc desktop -f "m-music"
super + n
    bspc desktop -f "n-school"

super + shift + q
    bspc node -d I
super + shift + w
    bspc node -d II
super + shift + e
    bspc node -d III
super + shift + r
    bspc node -d IV
super + shift + t
    bspc node -d "t-term"
super + shift + space
    bspc node -d "space-code"
super + shift + p
    bspc node -d "b-btop"
super + shift + y
    bspc node -d "y-testing"
super + shift + o
    bspc node -d "o-obsidian"
super + shift + b
    bspc node -d "b-firefox"
super + shift + a
    bspc node -d "a-chatgpt"
super + shift + g
    bspc node -d "g-github"
super + shift + v
    bspc node -d "v-monkeytype"
super + shift + m
    bspc node -d "m-music"
super + shift + n
    bspc node -d "n-school"

super + h
    bspc node -f west
super + j
    bspc node -f south
super + k
    bspc node -f north
super + l
    bspc node -f east

super + shift + h
    bspc node -s west
super + shift + j
    bspc node -s south
super + shift + k
    bspc node -s north
super + shift + l 
    bspc node -s east

super + alt + h
    bspc node -z left 20 0
super + alt + l
    bspc node -z right 20 0
super + alt + k
    bspc node -z top 0 20
super + alt + j
    bspc node -z bottom 0 20

super + ctrl + l
    bspc node -z left -20 0
super + ctrl + h
    bspc node -z right -20 0
super + ctrl + j
    bspc node -z top 0 -20
super + ctrl + k
    bspc node -z bottom 0 -20
